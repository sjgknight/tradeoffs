declare class Queue {
    speed: number;
    updates: (() => any)[];
    justProcessed: boolean;
    timeout: number;
    paused: boolean;
    constructor(speed?: number);
    schedule(update: () => any, waitIfProcessing?: boolean): void;
    pump(): void;
    pause(): void;
    resume(): void;
}
export default Queue;
//# sourceMappingURL=queue.d.ts.map