import React from 'react';
import type { User, UpdatePlayersMessage, GameSettings } from '../../Main.js';
declare const Seating: ({ users, players, minPlayers, maxPlayers, seatCount, onUpdatePlayers, onUpdateSettings }: {
    users: User[];
    players: User[];
    minPlayers: number;
    maxPlayers: number;
    seatCount: number;
    onUpdatePlayers: (operations: UpdatePlayersMessage['operations']) => void;
    onUpdateSettings: (update: {
        settings?: GameSettings | undefined;
        seatCount?: number | undefined;
    }) => void;
}) => React.JSX.Element;
export default Seating;
//# sourceMappingURL=Seating.d.ts.map