import React from 'react';
import DebugArgument from './DebugArgument.js';
const DebugChoices = ({ choices, heading }) => {
    if (!choices?.length)
        return null;
    return (React.createElement("li", null,
        heading,
        ":\u00A0",
        choices.map((c, i) => (React.createElement("span", { key: i },
            i ? ', ' : '',
            React.createElement(DebugArgument, { argument: (typeof c === 'object' ? ('choice' in c ? c.choice : ('option' in c ? c.option : undefined)) : undefined) ?? c }),
            typeof c === 'object' && 'error' in c && ` (${c.error})`)))));
};
export default DebugChoices;
//# sourceMappingURL=DebugChoices.js.map