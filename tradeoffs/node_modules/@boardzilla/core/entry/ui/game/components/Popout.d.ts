import React, { ReactNode } from 'react';
import type { UIRender } from '../../render.js';
declare const Popout: ({ layout, children, attributes }: {
    layout: UIRender['layouts'][number];
    children: Record<string, JSX.Element[]>;
    attributes: {
        button: ReactNode;
        popoutMargin?: number | {
            top: number;
            bottom: number;
            left: number;
            right: number;
        };
    };
}) => React.JSX.Element;
export default Popout;
//# sourceMappingURL=Popout.d.ts.map