import React from 'react';
import { gameStore } from '../../store.js';
import classNames from 'classnames';
/**
 * Component for rendering a Player's name and avatar in their color. Also
 * capture online status and displays (as `className` `"online"`) and flashes
 * when the player is the current player (`className` `"current"`).
 *
 * @category UI
 */
export function ProfileBadge({ player }) {
    const [userOnline] = gameStore(s => [s.userOnline]);
    const online = userOnline.has(player.id);
    return (React.createElement("div", { className: classNames("profile-badge", { online, current: player.isCurrent() }), "data-position": player.position, style: { backgroundColor: player.color } },
        React.createElement("div", { className: "avatar" },
            React.createElement("img", { src: player.avatar })),
        React.createElement("div", { className: "player-name" }, player.name)));
}
//# sourceMappingURL=ProfileBadge.js.map